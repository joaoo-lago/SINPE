services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: sinpe
      POSTGRES_PASSWORD: sinpe
      POSTGRES_DB: sinpe
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: sinpe
      MINIO_ROOT_PASSWORD: sinpe12345
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data

volumes:
  pgdata:
  miniodata:
